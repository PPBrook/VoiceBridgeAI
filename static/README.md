# Web 控制台（归档）

本目录为浏览器版 Web UI（`/`、`/config`）。**当前主开发线为 macOS App**。

完整浏览器版请 checkout **`legacy/web-only`**，见 [docs/web-legacy.md](../docs/web-legacy.md)。

开发模式下 `./run.sh` 仍可访问 http://127.0.0.1:8765/ 。
